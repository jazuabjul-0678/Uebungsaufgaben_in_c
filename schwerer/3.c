/*
Erstellen Sie eine Funktion, die die 'num'-Variable als Input-Parameter hat und dessen Wert um 3 verringert. Nennen Sie diese Funktion subtractBy3() oder was immer sie wollen. Wichtig ist, dass das "bevor"-printf() den Wert unverändert und das "danach"-printf() verändert ausgibt. Außerdem sollte die Funktion keinen Wert zurückgeben.
*/

#include <stdio.h>

// Die Funktion hier definieren...
// Wichtig: Nicht unter main()


int main() {
    int num = 33;

    printf("bevor: %d\n", num);

    // die Funktion hier aufrufen...


    printf("danach: %d\n", num);
}
