/*
Um einen String zu erstellen, verwendet man einen Array von chars. Da ein Array eigentlich nur ein Pointer ist (der zum ersten Element des Arrays zeigt), kann man einen String erstellen, indem man einem char-Pointer einen String Wert zordnet. Um einen String-Wert auszugeben, brauchst du keine Format Specifier (wie etwa "%d").

Erstellen Sie einen String "Hello World!" und geben Sie den Wert dann aus.
*/

#include <stdio.h>

int main() {


}
